static bool PCSrc;

void run(){
    /* get instruction that is at PC */

    /* PCSrc = EX/MEM(Zero & Branch) */
        /* if PCSrc == 0 --> No changes to the PC value */
        /* else PCsrc == 1 --> PC = EX/MEM BranchAddress */

    /* update IF/ID with the new instruction */
    /* update IF/ID with the  PC */

    /* PC = PC + 4 */
}
